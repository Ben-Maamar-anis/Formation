export interface ClientStats {
  totalClients: number;           // Total de clients
  activeClients: number;          // Clients actifs
  prospectClients: number;        // Prospects
  inactiveClients: number;        // Clients inactifs
  totalRevenue: number;           // Revenu total
  averageRevenue: number;         // Revenu moyen
  highPriorityClients: number;    // Clients haute priorité
}

